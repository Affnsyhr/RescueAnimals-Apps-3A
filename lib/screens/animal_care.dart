import 'package:flutter/material.dart';

class AnimalCareScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Animal Care'),
      ),
      body: Center(
        child: Text('Animal Care Screen'),
      ),
    );
  }
}
