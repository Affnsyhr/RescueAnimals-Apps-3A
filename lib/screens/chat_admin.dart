import 'package:flutter/material.dart';

class ChatAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Chat Admin'),
      ),
      body: Center(
        child: Text('Chat Admin Screen'),
      ),
    );
  }
}
